# Vagrant Cassandra

## 1.Base Template

TBD

vagrant up
vagrant ssh

name: base

has just python & java installed

go thru:

installs, point to docs
cassandra tar

dsc apt-get
opscenter + agent

simple walk-thru with
- nodetool status
- cqlsh
  create keyspace
  create table
  insert data
  select data

point to walk-thru docs
